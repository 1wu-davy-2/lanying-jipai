import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.lanying.jipai",
  appName: "蓝影寄拍",
  webDir: "dist",
  server: {
    androidScheme: "https",
  },
};

export default config;
